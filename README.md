# mygit — Minimal Version Control System in C++

**mygit** is a lightweight, Git-inspired version control system implemented in **C++**.  
It is designed as a learning project to demonstrate core VCS concepts such as **repositories, staging, commits, branches, and history**, while keeping the implementation simple and readable.
